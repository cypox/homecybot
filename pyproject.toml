[project]
name = "homecybot"
version = "0.1.0"
description = "Pair trading and statistical arbitrage bot for IB paper trading"
requires-python = ">=3.12"

[tool.pyright]
include = ["src", "tests"]
extraPaths = ["src"]
venvPath = "."
venv = ".venv"
